import { cartaoExportModel } from "@/lib/exporters/cartao-export-model";
import { holeriteExportModel } from "@/lib/exporters/holerite-export-model";
test("exporta cartão com página e pares de colunas", () => { const result = cartaoExportModel({ pages: [{ page: 1, days: [{ date_raw: "01/01/2020", punches: [{ kind: "IN", time_raw: "08:00", time_hhmm: "08:00" }, { kind: "OUT", time_raw: "12:00", time_hhmm: "12:00" }, { kind: "IN", time_raw: "13:00", time_hhmm: "13:00" }] }] }] }); expect(result.headers).toEqual(["Pág.", "Data", "Entrada 1", "Saída 1", "Entrada 2", "Saída 2"]); });
test("leva verbas, bases, referência e divergência do holerite", () => { const result = holeriteExportModel({ pages: [{ page: 1, month: "01", year: "2020", reference: "01/2020", divergence_calculo: true, fields: [{ code: "001", label: "Salário", reference: "220", value: "1,00", kind: "PROVENTO" }], bases: [{ label: "Base INSS", value: "1,00" }] }] }); expect(result.headers).toEqual(["Pág.", "Mês", "Ano", "Referência documento", "Divergência cálculo", "Tipo", "Código", "Descrição", "Referência", "Valor"]); expect(result.rows).toHaveLength(2); expect(result.rows[0]).toMatchObject({ "Referência documento": "01/2020", "Divergência cálculo": "Sim", Tipo: "PROVENTO" }); expect(result.rows[1]).toMatchObject({ Tipo: "Base de cálculo", "Descrição": "Base INSS" }); });
