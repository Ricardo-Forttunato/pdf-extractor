import { cartaoExportModel } from "@/lib/exporters/cartao-export-model";
import { holeriteExportModel } from "@/lib/exporters/holerite-export-model";
test("exporta cartão com pares de colunas", () => { const result = cartaoExportModel({ pages: [{ page: 1, days: [{ date_raw: "01/01/2020", punches: [{ kind: "IN", time_raw: "08:00", time_hhmm: "08:00" }, { kind: "OUT", time_raw: "12:00", time_hhmm: "12:00" }, { kind: "IN", time_raw: "13:00", time_hhmm: "13:00" }] }] }] }); expect(result.headers).toEqual(["Data", "Entrada 1", "Saída 1", "Entrada 2", "Saída 2"]); });
test("não leva bases para as colunas de holerite", () => { const result = holeriteExportModel({ pages: [{ page: 1, month: "01", year: "2020", fields: [{ code: "", label: "Salário", reference: "", value: "1,00" }], bases: [{ label: "Base INSS", value: "1,00" }] }] }); expect(result.headers).toEqual(["Pág.", "Mês", "Ano", "Salário"]); });
