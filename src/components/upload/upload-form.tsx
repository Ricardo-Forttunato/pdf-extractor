"use client";
import { Alert, Box, Button, FormControl, InputLabel, MenuItem, Select, Stack, Typography } from "@mui/material";
import { useState } from "react";
import { rememberPdf } from "@/components/upload/pdf-session";
import { TranscriptionStatus } from "@/components/upload/transcription-status";
import type { DocumentType } from "@/domain/transcription/model";
export function UploadForm() { const [file, setFile] = useState<File>(); const [tipo, setTipo] = useState<DocumentType>("cartao-ponto"); const [id, setId] = useState<string>(); const [error, setError] = useState<string>(); const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!file) { setError("Selecione um PDF."); return; } setError(undefined); const data = new FormData(); data.set("arquivo", file); data.set("tipo", tipo); try { const response = await fetch("/api/transcricoes", { method: "POST", body: data }); const body = await response.json() as { id?: string; erro?: string }; if (!response.ok || !body.id) throw new Error(body.erro ?? "Não foi possível enviar o documento."); rememberPdf(body.id, file); setId(body.id); } catch (cause) { setError(cause instanceof Error ? cause.message : "Não foi possível enviar o documento."); } };
  return <Box component="form" onSubmit={submit} sx={{ maxWidth: 560 }}><Stack spacing={3}><Typography variant="h4">Quick Filler</Typography><Typography>Envie um cartão de ponto ou holerite em PDF para revisar e exportar.</Typography><Button component="label" variant="outlined">{file ? "PDF selecionado" : "Selecionar PDF"}<input hidden type="file" accept="application/pdf,.pdf" onChange={(event) => setFile(event.target.files?.[0])} /></Button><FormControl><InputLabel id="tipo-label">Tipo</InputLabel><Select labelId="tipo-label" label="Tipo" value={tipo} onChange={(event) => setTipo(event.target.value as DocumentType)}><MenuItem value="cartao-ponto">Cartão de Ponto</MenuItem><MenuItem value="holerite">Holerite</MenuItem></Select></FormControl><Button type="submit" variant="contained" disabled={Boolean(id)}>Processar</Button>{error && <Alert severity="error">{error}</Alert>}{id && <TranscriptionStatus id={id} />}</Stack></Box>; }
